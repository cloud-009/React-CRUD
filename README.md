### Simple React CRUD with json-server

Clone the repository and run `npm install`.
Run the json-server using `json-server --watch db.json --port 4000`, make sure port 4000 is free, or you can change the port in the 
components.
